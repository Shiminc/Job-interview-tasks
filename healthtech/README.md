# Healthtech_machine_learning
I have been offered a large set of healthtech data (280MB) when interviewed for a job. Although I did not get the job, I continue to play with the data. 

The data_wrangling part showed how I explored the data and narrow down to a potential analysis with a subset of sample after some matching. 

The final data used is in the `data` folder, while the `scripts` contained two machine learning models I have run. 

